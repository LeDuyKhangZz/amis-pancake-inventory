{
  "name": "amis-pancake-inventory-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check api/sync.js && node --check api/cron.js && node --check api/misa-webhook.js && node --check api/github-sync.js && node --check lib/sync.js && node --check lib/github-oidc.js"
  }
}
